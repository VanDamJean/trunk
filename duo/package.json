{
  "name": "duo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "vocab:check": "node scripts/validate-vocab.mjs",
    "vocab:import": "node scripts/import-vocab-batch.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "^8.0.12",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "ts-fsrs": "^5.4.1"
  }
}
